---
interface Props{
    title:string,
    link: string,
}

const {title, link} = Astro.props
---

<div class="bg-grey-700 rounded-lg w-full h-[45px] flex justify-center pt-2.5">
    <a href={link} class="text-white font-semibold text-center">{title}</a>
</div>